{% import '_includes/forms' as forms %}

{{ forms.lightswitchField({
    label: 'Send confirmation mail',
    name: 'sendNotification',
    on: settings.sendNotification,
    instructions: 'Whether to send a confirmation mail to an email when it is first registered.',
    first: true
}) }}

{{ forms.lightswitchField({
    label: 'Use queue',
    name: 'useQueue',
    on: settings.useQueue,
    instructions: 'Whether to send mails via a background job.',
}) }}


{{ forms.editableTableField({
    label: "Lists"|t('emaillist'),
    name: 'lists',
    id: 'lists',
    cols: {
        label: {
            type: 'singleline',
            heading: 'Label'|t('emaillist'),
        },
        value: {
            type: 'singleline',
            heading: 'Handle'|t('emaillist'),
        }
    },
    rows: settings.lists,
    allowAdd: true,
    allowDelete: true,
    allowReorder: true,
    addRowLabel: "Add a list"|t('emaillist'),
}) }}